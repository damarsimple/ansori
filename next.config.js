/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["img.beritasatu.com", "picsum.photos", "asset.damaral.my.id"],
  },
};

module.exports = nextConfig;
